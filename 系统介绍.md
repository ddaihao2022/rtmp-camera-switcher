# RTMP 多摄影设备管理系统

## 概述

这是一套专为多机位直播场景设计的摄影设备管理平台，支持同时接入多路摄影设备，在一个界面内完成画面预览、切换、输出、录制与音频控制，满足演讲、演出、体育赛事、网络直播等各类现场制作需求。

---

## 主要功能

### 多路设备接入与监控

系统可同时接入任意数量的摄影设备或推流端。每路设备接入后，界面左侧列表会实时显示其在线状态与接入时间。操作人员可随时点击任意一路查看画面，也可切换至网格视图同时预览全部设备，掌握每个机位的实时情况。

每个直播画面的左上角会实时显示该路信号的延迟、缓冲时长、帧率和码率，方便操作人员即时判断信号质量。

### 画面切换与主输出

每路画面均可被指定为主输出（PROGRAM）。选定后，该画面即成为系统对外输出的正式信号源。右下角的 Program 监视窗口会始终显示当前主输出画面，方便操作人员在操作界面内随时确认输出内容而无需切换视图。

支持数字快捷键（1~9）一键切换主输出，适合节奏较快的现场导播操作。

### 本地媒体播放

除实时推流外，系统支持导入本地视频和音频文件作为素材源，与直播设备统一管理。本地媒体同样可选为主输出，支持设置循环播放、自动播放及播放速率，多个本地文件可按顺序自动播放。

### HDMI 全屏输出

在桌面客户端模式下，系统可检测当前连接的显示器，并将主输出画面以全屏方式投送至指定显示器（如接 HDMI 大屏或投影仪）。开启和关闭 HDMI 输出均可一键完成。

### 录制

系统支持对当前主输出画面直接录制。录制期间界面实时显示计时，停止后可选择保存路径，文件保存为通用视频格式，便于存档或后期剪辑。

### 音频混音台

系统内置独立的音频混音台窗口，可对每一路输入信号单独调节音量，并支持一键静音。混音台以频谱图和 VU 表直观展示每路信号的实时电平，方便声音工程师监控音量状态，避免过载或静音疏漏。

### 水印叠加

系统支持在主输出画面上叠加文字或图片水印。文字水印可自定义内容、颜色和字号；图片水印支持上传任意图片。水印的显示位置（左上、右上、左下、右下、居中）、透明度和边距均可灵活调整，所有设置实时生效。

---6+

## 适用场景

- 演讲、论坛、发布会等多机位直播活动
- 线下演出、体育赛事的现场导播
- 企业内部会议的多摄像头管理
- 网络直播间的多路信号切换
- 需要将画面实时投送至大屏幕的展览或展示场合

---

## 运行方式

系统提供桌面安装包（Windows），安装后直接运行，无需额外配置环境。摄影设备或推流软件（如 OBS）通过局域网推流至系统指定地址即可接入，操作人员通过主界面进行统一管理。
