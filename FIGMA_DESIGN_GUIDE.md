# RTMP多摄影设备管理系统 - Figma设计指南

## 项目概述
这是一个专业的RTMP多摄影设备直播管理系统，采用深色主题设计，具有现代感的毛玻璃效果和精致的动效。

---

## 1. 设计系统

### 1.1 颜色调色板

| 类别 | 颜色值 | 用途 |
|------|--------|------|
| **背景色** | `rgba(28, 28, 30, 0.72)` | 主要背景 |
| | `#000000` | 视频区域背景 |
| | `rgba(28, 28, 30, 0.55)` | 次级面板背景 |
| **主色** | `rgb(10, 132, 255)` | 主按钮、选中状态 |
| **成功色** | `rgb(52, 199, 89)` | 在线状态、输出中 |
| **警告色** | `rgb(245, 158, 11)` | 警告、HDMI |
| **危险色** | `rgb(239, 68, 68)` | 错误、停止 |
| **直播色** | `rgb(255, 59, 48)` | LIVE标识 |
| **紫色系** | `#667eea`, `#a78bfa` | 水印、本地媒体 |

### 1.2 字体系统
- 主要字体: Inter, -apple-system, BlinkMacSystemFont
- 代码字体: SF Mono, Monaco, Menlo, Consolas
- 字重: 400 (常规), 500 (中等), 600 (半粗), 700 (粗体)

### 1.3 圆角系统
- 小型: 4px, 5px
- 中型: 6px, 7px, 8px
- 大型: 10px, 12px, 14px
- 超大: 16px

### 1.4 阴影系统
- 轻度: `0 1px 2px rgba(0, 0, 0, 0.08)`
- 中度: `0 2px 8px rgba(0, 0, 0, 0.25)`
- 重度: `0 8px 32px rgba(0, 0, 0, 0.4)`

---

## 2. 主界面布局

### 2.1 整体结构
```
┌─────────────────────────────────────────────────────────────────────┐
│  顶部导航栏 (Header)                                                │
├──────────────┬──────────────────────────────────────────────────────┤
│              │                                                      │
│  左侧边栏    │                  主内容区                           │
│  (Sidebar)   │               (Main Content)                        │
│              │                                                      │
│  - 设备列表  │                                                      │
│  - 推流地址  │                                                      │
│  - HDMI/录制 │                                                      │
│              │                                                      │
└──────────────┴──────────────────────────────────────────────────────┘
                                                              [输出监视器]
```

### 2.2 顶部导航栏 (Header)
**组件**: Frame
- 背景填充: `rgba(28, 28, 30, 0.72)`
- 效果: 背景模糊40px，饱和度180%
- 内边距: 14px 32px
- 边框: 底部0.33px，`rgba(255, 255, 255, 0.12)`

**左侧标题**:
- 文字: "📹 RTMP多摄影设备管理系统"
- 字号: 22px
- 字重: 600
- 颜色: `rgba(255, 255, 255, 0.98)`

**右侧控件组**:
1. **状态指示器**:
   - 在线: 绿色圆形 `rgb(52, 199, 89)`，带脉冲动画
   - 离线: 红色圆形 `#ef4444`
2. **设备数徽章**:
   - 背景: `rgba(255, 255, 255, 0.1)`
   - 圆角: 8px
   - 边框: `rgba(255, 255, 255, 0.18)`
3. **功能按钮**:
   - 高度: 自适应
   - 圆角: 8px
   - 边框: `rgba(255, 255, 255, 0.2)`
   - 背景: `rgba(255, 255, 255, 0.06-0.12)`

### 2.3 左侧边栏 (Sidebar)
**组件**: Frame，宽度: 320px
- 背景: `rgba(28, 28, 30, 0.65)`
- 背景模糊: 40px
- 右边框: `rgba(255, 255, 255, 0.1)`

**区域1: 设备列表 (可滚动)**
- 标题: "摄影设备列表"，字号15px，字重600
- 流项卡片:
  - 背景: `rgba(255, 255, 255, 0.04)`
  - 圆角: 10px
  - 内边距: 12px 14px
  - 选中状态: 蓝色背景 `rgba(10, 132, 255, 0.15)` + 蓝色边框
  - 输出状态: 绿色边框 `rgba(52, 199, 89, 0.5)` + 绿色背景
  - 包含: 快捷键徽章、设备图标、设备名、时间、状态标签、输出按钮

**区域2: 底部固定面板**
- 推流地址面板
- HDMI输出面板
- 录制面板
- 每个面板都有标题和控件

### 2.4 主内容区 (Main Content)
**组件**: Frame，背景: `#000000`

**视图模式**:
1. **单屏视图**:
   - 视频播放器容器: 内边距24px 32px
   - 视频区域: 圆角12px，高度600px
   - 视频头部: 标题、LIVE徽章、操作按钮
   - 视频信息: 底部统计信息

2. **网格视图**:
   - 间距: 16px
   - 内边距: 16px
   - 网格项: 圆角14px，最小高度300px

### 2.5 输出监视器 (Output Monitor)
**组件**: Frame，浮动定位
- 位置: 右下角
- 宽度: 360px (折叠时220px)
- 背景: `rgba(28, 28, 30, 0.85)`
- 圆角: 12px
- 边框: `rgba(255, 255, 255, 0.14)`

**头部**:
- 背景: 渐变 `#1a1a1a` → `#141414`
- 左侧: 红色录制点 + "PROGRAM · [流名]"
- 右侧: 折叠按钮、关闭按钮

**视频区域**:
- 比例: 16:9
- 背景: `#000000`

---

## 3. 设置弹窗 (Settings Modal)

### 3.1 弹窗容器
- 宽度: 460px
- 最大宽度: 96vw
- 圆角: 14px
- 背景: `rgba(28, 28, 30, 0.94)`
- 背景模糊: 40px

### 3.2 分组结构
1. **HDMI输出**
2. **水印设置**
3. **调试日志**

每个分组有:
- 分组标题: 小字大写，颜色`#888888`
- 内容区域: 各种表单控件

### 3.3 控件样式

**开关 (Toggle)**:
- 轨道宽度: 40px，高度: 22px，圆角: 11px
- 关闭: `rgba(120, 120, 128, 0.32)`
- 开启: `rgb(52, 199, 89)`
- 滑块: 白色圆形，18px

**按钮 (Button)**:
- 主按钮: 蓝色背景 `rgb(10, 132, 255)`，圆角8px
- 危险按钮: 红色边框 + 红色文字
- Pill按钮: 圆角20px

**输入框 (Input)**:
- 背景: `rgba(0, 0, 0, 0.3-0.35)`
- 边框: `rgba(255, 255, 255, 0.14)`
- 圆角: 8px

---

## 4. Figma创建步骤建议

### 步骤1: 创建设计系统
1. 新建Frame作为画布 (建议: 1920x1080)
2. 创建颜色样式库
3. 创建文本样式库
4. 创建效果样式（阴影、模糊）

### 步骤2: 创建组件
1. 按钮组件（多种状态）
2. 流项卡片组件
3. 开关组件
4. 输入框组件

### 步骤3: 构建主界面
1. 先搭整体布局框架
2. 填充顶部导航
3. 构建左侧边栏
4. 构建主内容区
5. 添加输出监视器

### 步骤4: 创建变体
1. 单屏视图 vs 网格视图
2. 有设备 vs 无设备
3. 弹窗开启/关闭状态

---

## 5. 关键设计细节

### 5.1 毛玻璃效果
- 使用Figma的"背景模糊"效果
- 模糊值: 40px
- 饱和度: 180%

### 5.2 脉冲动画
- 用于状态指示器
- 不透明度在1和0.5之间循环

### 5.3 交互反馈
- 悬停: 按钮上移1px，背景变亮
- 选中: 高亮边框和背景
- 点击: 快速反馈

---

## 6. 页面尺寸参考
- 桌面主界面: 1920x1080
- 侧边栏宽度: 320px
- 弹窗宽度: 460px
- 输出监视器: 360px宽

祝您在Figma中复刻顺利！如有任何问题，请随时询问。
